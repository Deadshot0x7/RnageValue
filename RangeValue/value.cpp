class Rnagrvalue
{
    public:
    

    void num(int x, int n)
    {
        switch(x)
        {
            try {
                 case 1 :
                 for(int j =0 ; j<=n ; j++ )
                 {
                        ci>>j;
                 } 
   
        } catch (const std::exception& e) {
     std::cout << e.what(); // information from error printed
}

        }
    }
}