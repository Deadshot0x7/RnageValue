class Rnagrvalue
{
    public:
    

    void num(int x, int n)
    {
        switch(x)
        {
            try {
                 case 1 :
                 for(int j =0 ; j<=n ; j++ )
                 {
                        ci>>j;
                 } 
                 break
            case 2:
                
            
        } catch (const std::exception& e) {
     std::cout << e.what(); // information from error printed
}

        }
    }
}