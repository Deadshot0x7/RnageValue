class Rnagrvalue
{
    public:
    

    void num(int x, int n)
    {
        switch(x)
        {
            try {
                 case 1 :
                 for(int j =0 ; j<=n ; j++ )
                 {
                        cin>>j;
                 } 
                 break
            case 2:
                for (int j=0 j<=n; j++)
                {
                    if( )
                }
            
        } catch (const std::exception& e) {
     std::cout << e.what(); // information from error printed
}

        }
    }
}